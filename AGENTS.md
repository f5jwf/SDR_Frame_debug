# Releases

Increment `sdr_debug/version.py` for every delivered application version. Use a
minor increment for new features and a patch increment for fixes. Keep the
window title, package metadata, CHANGELOG and specifications in sync. Never
claim RF validation from synthetic tests alone.
